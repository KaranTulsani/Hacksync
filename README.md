# Social Automation

Automated social media posting using LLM and Selenium.

## Setup
1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```

## Usage
- Run the main pipeline:
  ```bash
  python main.py
  ```
- Run the daily scheduler:
  ```bash
  python -m scheduler.daily_scheduler
  ```
